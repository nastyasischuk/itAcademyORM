package CRUD.requests.DSLImpl;

import CRUD.requests.DSLInterfaces.*;

class AggregateImpl extends SimpleFieldImpl implements Aggregate {

    AggregateImpl(String name) {
        super(name);
    }
}
